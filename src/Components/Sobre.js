import React from "react";
import { Label } from "semantic-ui-react";

const Sobre = () => (
  <div
    style={{
      textAlign: "center"
    }}
  >
    <Label color={"blue"} style={{ marginTop: window.innerHeight * 0.5 }}>
      Sobre o sistema.
    </Label>
  </div>
);

export default Sobre;
